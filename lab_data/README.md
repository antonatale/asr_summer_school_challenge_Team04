# lab_data

One folder per day (`2026-09-11/`, ...). Each day folder has its own README listing the
files and the run that produced them. Maps, JSON logs and summaries are committed;
rosbags and images are gitignored (too large) and stay on the Mac.
